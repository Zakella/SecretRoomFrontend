export interface ProductVariant {
  appId?: number;
  size?: string;
  price?: number;
  oldPrice?: number;
  discountPercent?: number;
  discountAmount?: number;
  inStock?: boolean;
  available?: boolean;
}
